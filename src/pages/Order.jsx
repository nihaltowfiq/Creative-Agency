import { Fragment } from 'react';
import { Order as OrderComponent } from '../components/templates';

export const Order = () => {
    return (
        <Fragment>
            <OrderComponent />
        </Fragment>
    );
};
