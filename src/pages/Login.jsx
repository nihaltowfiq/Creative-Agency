import { Fragment } from 'react';
import { Login as LoginComponent } from '../components/templates';

export const Login = () => {
    return (
        <Fragment>
            <LoginComponent />
        </Fragment>
    );
};
